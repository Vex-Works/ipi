<Window x:Class="Ipi.Desktop.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ipi.Desktop"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Settings"
        Width="1080"
        Height="700"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        Foreground="{DynamicResource SettingsText}"
        FontFamily="{DynamicResource IpiTextFontFamily}">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="42" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>

  <Window.Resources>
    <FontFamily x:Key="IpiTextFontFamily">Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI, Microsoft YaHei, DengXian</FontFamily>
    <FontFamily x:Key="IpiCodeFontFamily">Cascadia Mono, Consolas, Segoe UI Mono, Microsoft YaHei UI</FontFamily>
    <FontFamily x:Key="IpiIconFontFamily">Segoe UI Symbol</FontFamily>

    <SolidColorBrush x:Key="SettingsBg" Color="#EEF1F7" />
    <SolidColorBrush x:Key="SettingsSidebarBg" Color="#E8EDF7" />
    <SolidColorBrush x:Key="SettingsCard" Color="#FFFFFF" />
    <SolidColorBrush x:Key="SettingsText" Color="#242733" />
    <SolidColorBrush x:Key="SettingsMuted" Color="#7A8190" />
    <SolidColorBrush x:Key="SettingsDim" Color="#9AA2B1" />
    <SolidColorBrush x:Key="SettingsLine" Color="#CCD4E5" />
    <SolidColorBrush x:Key="SettingsWindowBorder" Color="#AEB8CE" />
    <SolidColorBrush x:Key="SettingsControlBg" Color="#E8EDF9" />
    <SolidColorBrush x:Key="SettingsControlBorder" Color="#CBD4EA" />
    <SolidColorBrush x:Key="SettingsHoverBg" Color="#DDE4F3" />
    <SolidColorBrush x:Key="SettingsPressedBg" Color="#D1DAED" />
    <SolidColorBrush x:Key="SettingsPopupBg" Color="#F8FAFE" />
    <SolidColorBrush x:Key="SettingsSegmentBg" Color="#D8DEEE" />
    <SolidColorBrush x:Key="SettingsSidebarSelectedBg" Color="#D6DEEF" />
    <SolidColorBrush x:Key="SettingsFooterBg" Color="#F3F6FC" />
    <SolidColorBrush x:Key="SettingsPreviewBg" Color="#F7F9FE" />
    <SolidColorBrush x:Key="SettingsCaret" Color="#242733" />
    <SolidColorBrush x:Key="SettingsSelection" Color="#B9C8F6" />

    <Style TargetType="{x:Type ScrollViewer}">
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Focusable" Value="False" />
    </Style>

    <Style x:Key="SettingsGhostButton" TargetType="Button">
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Padding" Value="8,0" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsHoverBg}" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsPressedBg}" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
      <Setter Property="Width" Value="14" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ScrollBar}">
            <Grid Background="Transparent" Width="{TemplateBinding Width}">
              <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="False" /></Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb MinHeight="42" Background="{DynamicResource SettingsMuted}" Focusable="False">
                    <Thumb.Template>
                      <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Width="8" HorizontalAlignment="Center" Background="{TemplateBinding Background}" CornerRadius="4" Opacity="0.58" />
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="False" /></Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SettingsSidebarButton" TargetType="Button">
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Height" Value="30" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Padding" Value="10,0" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsHoverBg}" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsPressedBg}" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Background="{DynamicResource SettingsBg}" BorderBrush="{DynamicResource SettingsWindowBorder}" BorderThickness="1" SnapsToDevicePixels="True">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="42" />
        <RowDefinition Height="*" />
        <RowDefinition Height="74" />
      </Grid.RowDefinitions>

      <Border Grid.ColumnSpan="2" Background="{DynamicResource SettingsBg}" shell:WindowChrome.IsHitTestVisibleInChrome="False" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid>
          <TextBlock Text="{Binding WindowTitle}" Foreground="{DynamicResource SettingsDim}" FontSize="12" VerticalAlignment="Center" Margin="18,0,0,0" />
          <Button HorizontalAlignment="Right" Width="42" Height="42" Style="{StaticResource SettingsGhostButton}" Click="Close_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{Binding CloseText}">
            <local:LucideIcon Icon="x" Size="16" Stroke="{DynamicResource SettingsMuted}" HorizontalAlignment="Center" />
          </Button>
        </Grid>
      </Border>

      <Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Background="{DynamicResource SettingsSidebarBg}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,1,0">
        <StackPanel Margin="12,16,12,18">
          <Button Click="ModelsSettings_Click">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsModelsSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="settings" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding ModelsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="SkillsSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsSkillsSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="sparkles" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding SkillsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="PackagesSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsPackagesSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="plug" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding PluginPackagesTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="ArchivedSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsArchiveSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="archive" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding ArchivedConversationsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="AppearanceSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsAppearanceSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="palette" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding AppearanceTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="StorageSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsStorageSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="folder" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding StoragePathsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Click="DiagnosticsSettings_Click" Margin="0,6,0,0">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource SettingsSidebarButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsDiagnosticsSection}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SettingsControlBorder}" />
                    <Setter Property="BorderThickness" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <local:LucideIcon Icon="terminal-square" Size="16" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,10,0" />
              <TextBlock Text="{Binding RuntimeDiagnosticsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
        </StackPanel>
      </Border>

      <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0" Visibility="{Binding AppearancePageVisibility}">
        <ScrollViewer.Resources>
          <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                  <Grid Background="Transparent" Width="{TemplateBinding Width}">
                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                      <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="False" /></Track.DecreaseRepeatButton>
                      <Track.Thumb>
                        <Thumb MinHeight="42" Background="{DynamicResource SettingsMuted}" Focusable="False">
                          <Thumb.Template>
                            <ControlTemplate TargetType="{x:Type Thumb}">
                              <Border Width="4" HorizontalAlignment="Center" Background="{TemplateBinding Background}" CornerRadius="2" Opacity="0.45" />
                            </ControlTemplate>
                          </Thumb.Template>
                        </Thumb>
                      </Track.Thumb>
                      <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="False" /></Track.IncreaseRepeatButton>
                    </Track>
                  </Grid>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ScrollViewer.Resources>
        <Grid Margin="48,22,56,28">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <StackPanel Grid.Row="0" Margin="0,0,0,28">
            <StackPanel Orientation="Horizontal">
              <local:LucideIcon Icon="palette" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding AppearanceTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
            </StackPanel>
          </StackPanel>

          <Grid Grid.Row="1" Margin="0,0,0,28">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding LanguageTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,24,0" />
            <Grid Grid.Column="1" x:Name="LanguageMenuHost">
              <Button Width="168" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Padding="12,0" Click="LanguageMenu_Click">
                <Grid>
                  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <local:LucideIcon Icon="globe" Size="14" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,9,0" />
                    <TextBlock Text="{Binding LanguageLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" />
                  </StackPanel>
                  <local:LucideIcon Icon="chevron-down" Size="13" Stroke="{DynamicResource SettingsDim}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
              </Button>
              <Popup PlacementTarget="{Binding ElementName=LanguageMenuHost}" Placement="Bottom" VerticalOffset="6" AllowsTransparency="True" StaysOpen="False" IsOpen="{Binding IsLanguageMenuOpen, Mode=TwoWay}" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                <Border Width="168" Background="{DynamicResource SettingsPopupBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="7" Padding="6">
                  <ItemsControl ItemsSource="{Binding LanguageOptions}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Button Style="{StaticResource SettingsGhostButton}" Height="30" Padding="10,0" Click="LanguageOption_Click">
                          <TextBlock Text="{Binding Label}" Foreground="{DynamicResource SettingsText}" FontSize="13" VerticalAlignment="Center" />
                        </Button>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </Border>
              </Popup>
            </Grid>
          </Grid>

          <Grid Grid.Row="2" Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding ThemeTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,24,0" />
            <Border Grid.Column="1" Width="330" Height="30" Background="{DynamicResource SettingsSegmentBg}" CornerRadius="5" Padding="2">
              <ItemsControl ItemsSource="{Binding ModeOptions}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Button Height="26" Padding="8,0" Background="{Binding Background}" BorderBrush="{Binding BorderBrush}" BorderThickness="1" FocusVisualStyle="{x:Null}" Click="Mode_Click" Cursor="Hand">
                      <Button.Template>
                        <ControlTemplate TargetType="Button">
                          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                          </Border>
                        </ControlTemplate>
                      </Button.Template>
                      <StackPanel Orientation="Horizontal">
                        <local:LucideIcon Icon="{Binding Icon}" Size="12" Stroke="{Binding Foreground}" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding Label}" Foreground="{Binding Foreground}" FontSize="12" />
                      </StackPanel>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </Border>
          </Grid>

          <ItemsControl Grid.Row="3" ItemsSource="{Binding FilteredThemes}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><UniformGrid Columns="2" /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Style="{StaticResource SettingsGhostButton}" Click="Theme_Click" Padding="0" Margin="0,0,18,18" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                  <Border BorderBrush="{Binding CardBorderBrush}" BorderThickness="{Binding CardBorderThickness}" Background="{Binding CardBackground}" CornerRadius="3" Padding="12" HorizontalAlignment="Stretch">
                    <StackPanel>
                      <Border Height="116" Background="{Binding PreviewFrameBg}" BorderBrush="{Binding PreviewFrameBorder}" BorderThickness="1" CornerRadius="6" ClipToBounds="True">
                        <Grid>
                          <Grid.RowDefinitions>
                            <RowDefinition Height="12" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="13" />
                          </Grid.RowDefinitions>

                          <Border Grid.Row="0" Background="{Binding PreviewTitleBg}" BorderBrush="{Binding PreviewTitleBorder}" BorderThickness="0,0,0,1">
                            <Grid Margin="8,0">
                              <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="5" Height="5" BorderBrush="{Binding PreviewChromeBrush}" BorderThickness="1" CornerRadius="1" Margin="0,0,10,0" Opacity="0.72" />
                                <Border Width="18" Height="2" Background="{Binding PreviewChromeBrush}" CornerRadius="1" Margin="0,0,8,0" Opacity="0.45" />
                                <Border Width="18" Height="2" Background="{Binding PreviewChromeBrush}" CornerRadius="1" Margin="0,0,8,0" Opacity="0.35" />
                                <Border Width="26" Height="2" Background="{Binding PreviewChromeBrush}" CornerRadius="1" Opacity="0.6" />
                              </StackPanel>
                              <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Border Width="4" Height="4" Background="{Binding PreviewChromeBrush}" CornerRadius="2" Margin="0,0,8,0" Opacity="0.55" />
                                <Border Width="4" Height="4" Background="{Binding PreviewChromeBrush}" CornerRadius="2" Margin="0,0,8,0" Opacity="0.38" />
                                <Border Width="4" Height="4" Background="{Binding PreviewChromeBrush}" CornerRadius="2" Opacity="0.7" />
                              </StackPanel>
                            </Grid>
                          </Border>

                          <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="74" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>

                            <Border Background="{Binding PreviewSidebarBg}" BorderBrush="{Binding PreviewSidebarBorder}" BorderThickness="0,0,1,0">
                              <Grid Margin="10,8,8,8">
                                <StackPanel>
                                  <TextBlock Text="ipi" Foreground="{Binding PreviewSidebarText}" FontSize="17" FontWeight="SemiBold" FontFamily="Cascadia Mono, Consolas" />
                                  <Border Width="34" Height="3" Background="{Binding PreviewChromeBrush}" CornerRadius="1.5" HorizontalAlignment="Left" Margin="0,13,0,0" Opacity="0.46" />
                                  <Border Width="42" Height="3" Background="{Binding PreviewChromeBrush}" CornerRadius="1.5" HorizontalAlignment="Left" Margin="0,8,0,0" Opacity="0.32" />
                                  <Border Width="36" Height="3" Background="{Binding PreviewChromeBrush}" CornerRadius="1.5" HorizontalAlignment="Left" Margin="0,16,0,0" Opacity="0.28" />
                                </StackPanel>
                                <Border Width="50" Height="1" Background="{Binding PreviewChromeBrush}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Opacity="0.18" />
                              </Grid>
                            </Border>

                            <Grid Grid.Column="1" Background="{Binding PreviewWorkspaceBg}">

                              <Rectangle Width="70" Height="70" Fill="{Binding PreviewHeroText}" Opacity="0.82" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="-34,0,0,2" IsHitTestVisible="False">
                                <Rectangle.OpacityMask>
                                  <ImageBrush ImageSource="Assets/Brand/export/black/ipi-icon-black.png" Stretch="Uniform" />
                                </Rectangle.OpacityMask>
                              </Rectangle>
                              <TextBlock Text="ipi" Foreground="{Binding PreviewHeroText}" FontSize="31" FontWeight="Light" FontFamily="Cascadia Mono, Consolas" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="66,2,0,0" />
                              <TextBlock Text="Project · ...\ipi" Foreground="{Binding PreviewHeroDetail}" FontSize="6.5" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18,54,0,0" Opacity="0.84" />

                              <Border Height="14" Width="188" Background="{Binding PreviewComposerBg}" BorderBrush="{Binding PreviewComposerBorder}" BorderThickness="1" CornerRadius="7" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,8" Opacity="0.96">
                                <Grid Margin="8,0">
                                  <TextBlock Text="+" Foreground="{Binding PreviewChromeBrush}" FontSize="9" VerticalAlignment="Center" />
                                  <local:LucideIcon Icon="arrow-up" Size="9" Stroke="{Binding PreviewComposerIcon}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>
                              </Border>
                            </Grid>
                          </Grid>

                          <Border Grid.Row="2" Background="{Binding PreviewFooterBg}" BorderBrush="{Binding PreviewTitleBorder}" BorderThickness="0,1,0,0">
                            <Grid Margin="7,0">
                              <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="5" Height="5" BorderBrush="{Binding PreviewChromeBrush}" BorderThickness="1" CornerRadius="1" Margin="0,0,6,0" Opacity="0.58" />
                                <Border Width="38" Height="2" Background="{Binding PreviewChromeBrush}" CornerRadius="1" Opacity="0.42" />
                              </StackPanel>
                              <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Border Width="20" Height="2" Background="{Binding PreviewChromeBrush}" CornerRadius="1" Margin="0,0,8,0" Opacity="0.32" />
                                <Border Width="24" Height="2" Background="{Binding PreviewHeroText}" CornerRadius="1" Opacity="0.82" />
                              </StackPanel>
                            </Grid>
                          </Border>
                        </Grid>
                      </Border>
                      <TextBlock Text="{Binding Name}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" Margin="4,16,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </Border>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
      </ScrollViewer>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding ArchivePageVisibility}" Margin="52,44,62,36">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,28">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
              <local:LucideIcon Icon="archive" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding ArchivedConversationsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
            </StackPanel>
            <TextBlock Text="{Binding ArchivedConversationsDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" />
          </StackPanel>
          <Button Grid.Column="1" Width="96" Height="32" Margin="0,0,10,0" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="CleanMissingArchived_Click">
            <TextBlock Text="{Binding CleanMissingArchivedText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" />
          </Button>
          <Button Grid.Column="2" Width="96" Height="32" Margin="0,0,10,0" Style="{StaticResource SettingsGhostButton}" Background="#18FF4D4D" BorderBrush="#33FF4D4D" BorderThickness="1" Click="DeleteAllArchived_Click">
            <TextBlock Text="{Binding DeleteAllArchivedText}" Foreground="#E35B55" FontSize="12" />
          </Button>
          <Button Grid.Column="3" Width="96" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="RestoreAllArchived_Click">
            <TextBlock Text="{Binding RestoreAllArchivedText}" Foreground="{DynamicResource SettingsText}" FontSize="12" />
          </Button>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,14">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <Border Height="36" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="7" Padding="12,0">
            <Grid>
              <StackPanel Orientation="Horizontal" IsHitTestVisible="False" VerticalAlignment="Center">
                <local:LucideIcon Icon="search" Size="15" Stroke="{DynamicResource SettingsDim}" Margin="0,0,10,0" />
                <TextBlock Text="{Binding ArchiveSearchPlaceholder}" Foreground="{DynamicResource SettingsDim}" FontSize="13">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Visibility" Value="Collapsed" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding ArchiveSearchText}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <TextBox Text="{Binding ArchiveSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" FontSize="13" VerticalContentAlignment="Center" Margin="25,0,0,0" />
            </Grid>
          </Border>
          <TextBlock Grid.Column="1" Text="{Binding ArchivedCountText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" VerticalAlignment="Center" Margin="16,0,0,0" />
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
          <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ArchivedEmptyVisibility}">
              <local:LucideIcon Icon="archive" Size="26" Stroke="{DynamicResource SettingsDim}" HorizontalAlignment="Center" Margin="0,0,0,14" />
              <TextBlock Text="{Binding ArchivedEmptyTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" HorizontalAlignment="Center" />
              <TextBlock Text="{Binding ArchivedEmptyDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Width="420" Margin="0,10,0,0" />
            </StackPanel>

            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding ArchivedListVisibility}">
              <ItemsControl ItemsSource="{Binding ArchivedSessions}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="18,14">
                      <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="0" /><ColumnDefinition Width="260" /></Grid.ColumnDefinitions>
                        <Grid>
                          <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                          <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="25" /><ColumnDefinition Width="*" MinWidth="0" /></Grid.ColumnDefinitions>
                            <local:LucideIcon Icon="message-square" Size="15" Stroke="{DynamicResource SettingsMuted}" Margin="0,2,10,0" />
                            <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" TextTrimming="CharacterEllipsis" />
                          </Grid>
                          <TextBlock Grid.Row="1" Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextTrimming="CharacterEllipsis" />
                          <TextBlock Grid.Row="2" Text="{Binding ProjectPath}" Foreground="{DynamicResource SettingsDim}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="25,7,0,0" />
                        </Grid>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="18,0,0,0">
                          <TextBlock Text="{Binding ExistsText}" Foreground="{DynamicResource SettingsDim}" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0" />
                          <Button Width="66" Height="30" Margin="0,0,8,0" Style="{StaticResource SettingsGhostButton}" Background="#18FF4D4D" BorderBrush="#33FF4D4D" BorderThickness="1" Click="DeleteArchived_Click">
                            <TextBlock Text="{Binding DeleteText}" Foreground="#E35B55" FontSize="12" />
                          </Button>
                          <Button Width="66" Height="30" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="RestoreArchived_Click">
                            <TextBlock Text="{Binding RestoreText}" Foreground="{DynamicResource SettingsText}" FontSize="12" />
                          </Button>
                        </StackPanel>
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Grid>
        </Border>
      </Grid>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding ModelsPageVisibility}" Margin="36,34,42,30">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
              <local:LucideIcon Icon="settings" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding ModelsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
              <TextBlock Text="{Binding ModelsPathText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="14,4,0,0" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <TextBlock Text="{Binding ModelsDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" />
          </StackPanel>
          <Button Grid.Column="1" Width="132" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddProvider_Click">
            <TextBlock Text="{Binding AddProviderText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
          </Button>
        </Grid>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="286" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <Border Grid.Column="0" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="58" /></Grid.RowDefinitions>
              <TextBlock Text="{Binding ProviderCountText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" FontWeight="SemiBold" Margin="16,14,16,10" />
              <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ModelProviderEmptyVisibility}">
                <local:LucideIcon Icon="settings" Size="26" Stroke="{DynamicResource SettingsDim}" HorizontalAlignment="Center" Margin="0,0,0,14" />
                <TextBlock Text="{Binding ModelProviderEmptyTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" HorizontalAlignment="Center" />
                <TextBlock Text="{Binding ModelProviderEmptyDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Width="220" Margin="0,9,0,0" />
              </StackPanel>
              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding ModelProviderListVisibility}">
                <ItemsControl ItemsSource="{Binding ModelProviders}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Button Click="ModelProvider_Click">
                        <Button.Style>
                          <Style TargetType="Button" BasedOn="{StaticResource SettingsGhostButton}">
                            <Setter Property="Padding" Value="10" />
                            <Setter Property="Margin" Value="8,2" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsControlBg}" /></DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Button.Style>
                        <Grid>
                          <Grid.ColumnDefinitions><ColumnDefinition Width="22" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                          <local:ProviderBrandIcon Provider="{Binding Provider}" Label="{Binding Title}" Size="16" Foreground="{DynamicResource SettingsMuted}" Margin="0,1,0,0" />
                          <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" />
                          </StackPanel>
                        </Grid>
                      </Button>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
              <Border Grid.Row="2" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,1,0,0" Padding="10">
                <Button Height="36" Style="{StaticResource SettingsGhostButton}" Background="Transparent" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddProvider_Click">
                  <TextBlock Text="{Binding AddProviderText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" HorizontalAlignment="Center" />
                </Button>
              </Border>
            </Grid>
          </Border>

          <Border Grid.Column="2" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" Padding="22" Visibility="{Binding ModelProviderDetailVisibility}">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
              <StackPanel Margin="0,0,0,18">
                <TextBlock Text="{Binding SelectedProviderTitle}" Foreground="{DynamicResource SettingsText}" FontSize="16" FontWeight="Medium" />
                <TextBlock Text="{Binding SelectedProviderDetail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,8,0,0" />
              </StackPanel>
              <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ModelDetailEmptyVisibility}">
                <TextBlock Text="{Binding ModelDetailEmptyTitle}" Foreground="{DynamicResource SettingsMuted}" FontSize="13" HorizontalAlignment="Center" />
                <TextBlock Text="{Binding ModelDetailEmptyDescription}" Foreground="{DynamicResource SettingsDim}" FontSize="12" HorizontalAlignment="Center" Margin="0,8,0,0" />
              </StackPanel>
              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding SelectedProviderModelsVisibility}">
                <ItemsControl ItemsSource="{Binding SelectedProviderModels}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="0,12">
                        <Grid>
                          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                          <StackPanel>
                            <TextBlock Text="{Binding Name}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" />
                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,5,0,0" />
                          </StackPanel>
                          <TextBlock Grid.Column="1" Text="{Binding StateText}" Foreground="{DynamicResource SettingsDim}" FontSize="12" VerticalAlignment="Center" Margin="18,0,0,0" />
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
            </Grid>
          </Border>

          <Border Grid.Column="0" Grid.ColumnSpan="3" Background="{DynamicResource SettingsPopupBg}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" Padding="22" Visibility="{Binding AddProviderVisibility}">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
              <Grid Height="0" Visibility="Collapsed" />

              <Grid Grid.Row="1" Visibility="{Binding ProviderGalleryVisibility}">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                <Grid Margin="0,0,0,18">
                  <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                  <Border Height="36" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="8" Padding="12,0">
                    <Grid>
                      <StackPanel Orientation="Horizontal" IsHitTestVisible="False" VerticalAlignment="Center">
                        <local:LucideIcon Icon="search" Size="14" Stroke="{DynamicResource SettingsDim}" Margin="0,0,10,0" />
                        <TextBlock Text="{Binding ProviderSearchPlaceholder}" Foreground="{DynamicResource SettingsDim}" FontSize="13">
                          <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding ProviderSearchText}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                        </TextBlock>
                      </StackPanel>
                      <TextBox Text="{Binding ProviderSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" FontSize="13" VerticalContentAlignment="Center" Margin="25,0,0,0" />
                    </Grid>
                  </Border>
                  <Button Grid.Column="1" Width="76" Height="36" Margin="12,0,0,0" Style="{StaticResource SettingsGhostButton}" Click="CancelAddProvider_Click">
                    <TextBlock Text="{Binding CancelText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" />
                  </Button>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                  <ItemsControl ItemsSource="{Binding FilteredProviderTemplateGroups}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <StackPanel Margin="0,0,0,18">
                          <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsDim}" FontSize="11" FontWeight="Medium" Margin="0,0,0,9" />
                          <ItemsControl ItemsSource="{Binding Items}">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                              <DataTemplate>
                                <Button Click="ProviderTemplate_Click" Margin="0,0,12,12" FocusVisualStyle="{x:Null}">
                                  <Button.Style>
                                    <Style TargetType="Button">
                                      <Setter Property="Padding" Value="0" />
                                      <Setter Property="HorizontalAlignment" Value="Stretch" />
                                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                      <Setter Property="Height" Value="82" />
                                      <Setter Property="Background" Value="Transparent" />
                                      <Setter Property="BorderBrush" Value="Transparent" />
                                      <Setter Property="Cursor" Value="Hand" />
                                      <Setter Property="Template">
                                        <Setter.Value>
                                          <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                              <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                              <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsHoverBg}" /></Trigger>
                                              <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="{DynamicResource SettingsPressedBg}" /></Trigger>
                                            </ControlTemplate.Triggers>
                                          </ControlTemplate>
                                        </Setter.Value>
                                      </Setter>
                                    </Style>
                                  </Button.Style>
                                  <Border Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="8" Padding="16,14">
                                    <Grid>
                                      <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="44" /></Grid.ColumnDefinitions>
                                      <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="Medium" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,7,0,0" TextTrimming="CharacterEllipsis" />
                                      </StackPanel>
                                      <Border Grid.Column="1" Width="34" Height="34" CornerRadius="17" Background="{DynamicResource SettingsPreviewBg}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <local:ProviderBrandIcon Provider="{Binding ProviderId}" Label="{Binding Title}" Size="20" Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                      </Border>
                                    </Grid>
                                  </Border>
                                </Button>
                              </DataTemplate>
                            </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </StackPanel>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </ScrollViewer>
              </Grid>

              <Grid Grid.Row="1" Visibility="{Binding ProviderConfigVisibility}">
                <Grid.ColumnDefinitions><ColumnDefinition Width="120" /><ColumnDefinition Width="420" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Button Width="82" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Style="{StaticResource SettingsGhostButton}" Click="CancelAddProvider_Click">
                  <StackPanel Orientation="Horizontal"><local:LucideIcon Icon="arrow-left" Size="13" Stroke="{DynamicResource SettingsMuted}" Margin="0,0,6,0" /><TextBlock Text="{Binding CancelText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" /></StackPanel>
                </Button>
                <StackPanel Grid.Column="1">
                  <TextBlock Text="{Binding CustomProviderFormTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" Margin="0,0,0,16" />
                  <TextBlock Text="{Binding NewProviderIdLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,0,0,6" />
                  <TextBox Text="{Binding NewProviderId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" />
                  <TextBlock Text="{Binding NewProviderBaseUrlLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,12,0,6" />
                  <TextBox Text="{Binding NewProviderBaseUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" />
                  <TextBlock Text="{Binding NewProviderApiLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,12,0,6" />
                  <TextBox Text="{Binding NewProviderApi, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" />
                  <TextBlock Text="{Binding NewProviderApiKeyRefLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,12,0,6" />
                  <TextBox Text="{Binding NewProviderApiKeyRef, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" />
                  <TextBlock Text="{Binding NewProviderModelsLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,12,0,6" Visibility="{Binding NewProviderModelsVisibility}" />
                  <TextBox Text="{Binding NewProviderModelIds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="78" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,8" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Visibility="{Binding NewProviderModelsVisibility}" />
                  <TextBlock Text="{Binding NewProviderStatus}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="{Binding NewProviderStatusVisibility}" />
                  <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                    <Button Width="142" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="SaveProvider_Click">
                      <TextBlock Text="{Binding SaveProviderText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
                    </Button>
                  </StackPanel>
                </StackPanel>
              </Grid>
            </Grid>
          </Border>
        </Grid>
      </Grid>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding SkillsPageVisibility}" Margin="52,44,62,36">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,20">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
              <local:LucideIcon Icon="sparkles" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding SkillsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
              <TextBlock Text="{Binding SkillsSummary}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="14,4,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding SkillsDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
          </StackPanel>
          <Button Grid.Column="1" Width="104" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddSkillSource_Click">
            <TextBlock Text="{Binding AddSkillSourceText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
          </Button>
        </Grid>
        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions><ColumnDefinition Width="300" /><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
          <Border Grid.Column="0" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
              <ItemsControl ItemsSource="{Binding SkillSources}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border x:Name="SourceRow" Height="78" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Background="Transparent" Padding="14,10" ClipToBounds="True" Cursor="Hand" MouseLeftButtonUp="SkillSourceRow_Click">
                      <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="52" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                          <TextBlock Text="{Binding Label}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                          <TextBlock Text="{Binding Path}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,8,8,0" />
                        </StackPanel>
                        <Button Grid.Column="1" Width="48" Height="30" Padding="0" BorderThickness="0" Background="Transparent" Style="{StaticResource SettingsGhostButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="SkillSourceToggle_Click">
                          <Border x:Name="SourceSwitchBg" Width="42" Height="24" CornerRadius="12" Background="{DynamicResource SettingsLine}">
                            <Border x:Name="SourceSwitchKnob" Width="18" Height="18" CornerRadius="9" Background="{DynamicResource SettingsBg}" HorizontalAlignment="Left" Margin="3,0,0,0" />
                          </Border>
                        </Button>
                      </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                      <DataTrigger Binding="{Binding IsSelected}" Value="True">
                        <Setter TargetName="SourceRow" Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                      </DataTrigger>
                      <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                        <Setter TargetName="SourceSwitchBg" Property="Background" Value="{DynamicResource SettingsText}" />
                        <Setter TargetName="SourceSwitchKnob" Property="HorizontalAlignment" Value="Right" />
                        <Setter TargetName="SourceSwitchKnob" Property="Margin" Value="0,0,3,0" />
                      </DataTrigger>
                    </DataTemplate.Triggers>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Border>
          <Border Grid.Column="2" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="44" /><RowDefinition Height="*" /></Grid.RowDefinitions>
              <Border Grid.Row="0" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="18,0">
                <Grid>
                  <TextBlock Text="{Binding SelectedSkillSourceTitle}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                  <TextBlock Text="{Binding SkillsSummary}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>
              </Border>
              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding SkillItems}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border Height="98" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="18,12" ClipToBounds="True">
                        <Grid>
                          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="60" /></Grid.ColumnDefinitions>
                          <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,18,0">
                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding StateText}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" Margin="0,4,0,0" />
                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" Height="34" ClipToBounds="True" Margin="0,7,0,0" />
                          </StackPanel>
                          <Button Grid.Column="1" Width="50" Height="32" Padding="0" BorderThickness="0" Background="Transparent" Style="{StaticResource SettingsGhostButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="SkillToggle_Click">
                            <Border x:Name="SkillSwitchBg" Width="42" Height="24" CornerRadius="12" Background="{DynamicResource SettingsLine}">
                              <Border x:Name="SkillSwitchKnob" Width="18" Height="18" CornerRadius="9" Background="{DynamicResource SettingsBg}" HorizontalAlignment="Left" Margin="3,0,0,0" />
                            </Border>
                          </Button>
                        </Grid>
                      </Border>
                      <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                          <Setter TargetName="SkillSwitchBg" Property="Background" Value="{DynamicResource SettingsText}" />
                          <Setter TargetName="SkillSwitchKnob" Property="HorizontalAlignment" Value="Right" />
                          <Setter TargetName="SkillSwitchKnob" Property="Margin" Value="0,0,3,0" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding SourceEnabled}" Value="False">
                          <Setter TargetName="SkillSwitchBg" Property="Opacity" Value="0.42" />
                          <Setter TargetName="SkillSwitchKnob" Property="Opacity" Value="0.58" />
                        </DataTrigger>
                      </DataTemplate.Triggers>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
            </Grid>
          </Border>
        </Grid>
      </Grid>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding PluginPackagesPageVisibility}" Margin="52,44,62,36">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,20">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
              <local:LucideIcon Icon="plug" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding PluginPackagesTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
              <TextBlock Text="{Binding PluginPackagesSummary}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="14,4,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding PluginPackagesDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
            <TextBlock Text="{Binding PluginPackagesSettingsPath}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" FontFamily="{DynamicResource IpiCodeFontFamily}" TextTrimming="CharacterEllipsis" Margin="0,8,0,0" />
          </StackPanel>
          <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
            <Button Width="82" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddPackageOpen_Click" IsEnabled="{Binding PackageActionButtonEnabled}" Margin="0,0,8,0">
              <TextBlock Text="{Binding PackageAddButtonText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
            </Button>
            <Button Width="92" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="UpdatePackage_Click" IsEnabled="{Binding PackageActionButtonEnabled}" Margin="0,0,8,0">
              <TextBlock Text="{Binding PackageUpdateButtonText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
            </Button>
            <Button Width="108" Height="32" Style="{StaticResource SettingsGhostButton}" Background="#18FF4D4D" BorderBrush="#33FF4D4D" BorderThickness="1" Click="RemovePackage_Click" IsEnabled="{Binding PackageActionButtonEnabled}" Margin="0,0,8,0">
              <TextBlock Text="{Binding PackageRemoveButtonText}" Foreground="#D94A45" FontSize="13" />
            </Button>
            <Button Width="92" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="RefreshPackages_Click" IsEnabled="{Binding PackageActionButtonEnabled}">
              <TextBlock Text="{Binding RefreshPackagesText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
            </Button>
          </StackPanel>
        </Grid>
        <Border Grid.Row="1" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" Padding="14,12" Margin="0,0,0,14" Visibility="{Binding PackageAddVisibility}">
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="130" /><ColumnDefinition Width="*" /><ColumnDefinition Width="138" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock Text="{Binding PackageInstallSourceLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" Text="{Binding PackageNewSource, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="32" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" FontFamily="{DynamicResource IpiCodeFontFamily}" ToolTip="{Binding PackageSourcePlaceholder}" />
            <Border Grid.Column="2" Height="32" Margin="10,0,0,0" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="7" Padding="3" ToolTip="{Binding PackageInstallScopeDetail}">
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Button Grid.Column="0" Height="24" Click="PackageScopeGlobal_Click">
                  <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource SettingsGhostButton}">
                      <Setter Property="Background" Value="Transparent" />
                      <Setter Property="BorderThickness" Value="0" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding PackageInstallScope}" Value="global">
                          <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                  <TextBlock Text="{Binding PackageGlobalScopeText}" Foreground="{DynamicResource SettingsText}" FontSize="12" HorizontalAlignment="Center" />
                </Button>
                <Button Grid.Column="1" Height="24" Click="PackageScopeProject_Click">
                  <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource SettingsGhostButton}">
                      <Setter Property="Background" Value="Transparent" />
                      <Setter Property="BorderThickness" Value="0" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding PackageInstallScope}" Value="project">
                          <Setter Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                  <TextBlock Text="{Binding PackageProjectScopeText}" Foreground="{DynamicResource SettingsText}" FontSize="12" HorizontalAlignment="Center" />
                </Button>
              </Grid>
            </Border>
            <Button Grid.Column="3" Width="76" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddPackageConfirm_Click" IsEnabled="{Binding PackageActionButtonEnabled}" Margin="10,0,0,0">
              <TextBlock Text="{Binding PackageAddButtonText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
            </Button>
            <Button Grid.Column="4" Width="76" Height="32" Style="{StaticResource SettingsGhostButton}" Background="Transparent" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="AddPackageCancel_Click" IsEnabled="{Binding PackageActionButtonEnabled}" Margin="8,0,0,0">
              <TextBlock Text="{Binding PackageCancelButtonText}" Foreground="{DynamicResource SettingsMuted}" FontSize="13" />
            </Button>
          </Grid>
        </Border>
        <TextBlock Grid.Row="1" Text="{Binding PackageActionStatus}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,-10,0,12" VerticalAlignment="Bottom" Visibility="{Binding PackageActionStatusVisibility}" />
        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions><ColumnDefinition Width="320" /><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
          <Border Grid.Column="0" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
            <Grid>
              <TextBlock Text="{Binding PluginPackagesEmptyText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding PluginPackagesEmptyVisibility}" />
              <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding PluginPackagesDetailVisibility}">
                <ItemsControl ItemsSource="{Binding SettingsPluginPackages}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border x:Name="PackageRow" Height="84" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Background="Transparent" Padding="14,10" ClipToBounds="True" Cursor="Hand" MouseLeftButtonUp="SettingsPluginPackageRow_Click">
                        <Grid>
                          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="52" /></Grid.ColumnDefinitions>
                          <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Source}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" FontFamily="{DynamicResource IpiCodeFontFamily}" TextTrimming="CharacterEllipsis" Margin="0,6,8,0" />
                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,4,8,0" />
                          </StackPanel>
                          <Button Grid.Column="1" Width="48" Height="30" Padding="0" BorderThickness="0" Background="Transparent" Style="{StaticResource SettingsGhostButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="SettingsPluginPackageToggle_Click">
                            <Border x:Name="PackageSwitchBg" Width="42" Height="24" CornerRadius="12" Background="{DynamicResource SettingsLine}">
                              <Border x:Name="PackageSwitchKnob" Width="18" Height="18" CornerRadius="9" Background="{DynamicResource SettingsBg}" HorizontalAlignment="Left" Margin="3,0,0,0" />
                            </Border>
                          </Button>
                        </Grid>
                      </Border>
                      <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                          <Setter TargetName="PackageRow" Property="Background" Value="{DynamicResource SettingsSidebarSelectedBg}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Disabled}" Value="False">
                          <Setter TargetName="PackageSwitchBg" Property="Background" Value="{DynamicResource SettingsText}" />
                          <Setter TargetName="PackageSwitchKnob" Property="HorizontalAlignment" Value="Right" />
                          <Setter TargetName="PackageSwitchKnob" Property="Margin" Value="0,0,3,0" />
                        </DataTrigger>
                      </DataTemplate.Triggers>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
            </Grid>
          </Border>
          <Border Grid.Column="2" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True" Visibility="{Binding PluginPackagesDetailVisibility}">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
              <Border Grid.Row="0" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="18,16">
                <StackPanel>
                  <TextBlock Text="{Binding SelectedPluginPackageTitle}" Foreground="{DynamicResource SettingsText}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                  <TextBlock Text="{Binding SelectedPluginPackageSource}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" FontFamily="{DynamicResource IpiCodeFontFamily}" TextTrimming="CharacterEllipsis" Margin="0,7,0,0" />
                  <UniformGrid Columns="2" Margin="0,16,0,0">
                    <StackPanel Margin="0,0,16,10"><TextBlock Text="{Binding PackageStatusLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" /><TextBlock Text="{Binding SelectedPluginPackageStatus}" Foreground="{DynamicResource SettingsText}" FontSize="13" Margin="0,4,0,0" /></StackPanel>
                    <StackPanel Margin="0,0,16,10"><TextBlock Text="{Binding PackageVersionLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" /><TextBlock Text="{Binding SelectedPluginPackageVersion}" Foreground="{DynamicResource SettingsText}" FontSize="13" Margin="0,4,0,0" /></StackPanel>
                    <StackPanel Margin="0,0,16,10"><TextBlock Text="{Binding PackageResourcesLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" /><TextBlock Text="{Binding SelectedPluginPackageResources}" Foreground="{DynamicResource SettingsText}" FontSize="13" Margin="0,4,0,0" /></StackPanel>
                    <StackPanel Margin="0,0,16,10"><TextBlock Text="{Binding PackageScopeLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" /><TextBlock Text="{Binding SelectedPluginPackageScope}" Foreground="{DynamicResource SettingsText}" FontSize="13" Margin="0,4,0,0" /></StackPanel>
                  </UniformGrid>
                  <TextBlock Text="{Binding SelectedPluginPackagePath}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" FontFamily="{DynamicResource IpiCodeFontFamily}" TextWrapping="Wrap" Margin="0,4,0,0" />
                </StackPanel>
              </Border>
              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding SettingsPluginResourceGroups}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <StackPanel Margin="18,16,18,4">
                        <TextBlock Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Items}">
                          <ItemsControl.ItemTemplate>
                            <DataTemplate>
                              <Border BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="0,8">
                                <Grid>
                                  <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                  <TextBlock Text="{Binding Name}" Foreground="{DynamicResource SettingsText}" FontSize="12" TextTrimming="CharacterEllipsis" />
                                  <TextBlock Grid.Column="1" Text="{Binding Path}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" FontFamily="{DynamicResource IpiCodeFontFamily}" TextTrimming="CharacterEllipsis" />
                                </Grid>
                              </Border>
                            </DataTemplate>
                          </ItemsControl.ItemTemplate>
                        </ItemsControl>
                      </StackPanel>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </ScrollViewer>
            </Grid>
          </Border>
        </Grid>
      </Grid>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding StoragePathsPageVisibility}" Margin="52,44,62,36">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
          <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <local:LucideIcon Icon="folder" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
            <TextBlock Text="{Binding StoragePathsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
          </StackPanel>
          <TextBlock Text="{Binding StoragePathsDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
          <TextBlock Text="{Binding StorageConfigPathText}" Foreground="{DynamicResource SettingsDim}" FontSize="11" FontFamily="{DynamicResource IpiCodeFontFamily}" TextTrimming="CharacterEllipsis" Margin="0,10,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" Padding="22,20">
          <StackPanel>
            <TextBlock Text="{Binding AppDataDirTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="SemiBold" />
            <TextBlock Text="{Binding AppDataDirDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,10" />
            <Grid Margin="0,0,0,22">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="92" /><ColumnDefinition Width="82" /></Grid.ColumnDefinitions>
              <TextBox Grid.Column="0" Text="{Binding AppDataDirOverride, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" FontFamily="{DynamicResource IpiCodeFontFamily}" ToolTip="{Binding EffectiveAppDataDir}" />
              <Button Grid.Column="1" Margin="10,0,0,0" Height="34" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="BrowseAppDataDir_Click"><TextBlock Text="{Binding BrowseText}" Foreground="{DynamicResource SettingsText}" FontSize="12" /></Button>
              <Button Grid.Column="2" Margin="10,0,0,0" Height="34" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="ResetAppDataDir_Click"><TextBlock Text="{Binding RestoreDefaultText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" /></Button>
            </Grid>

            <TextBlock Text="{Binding LocalAppDataDirTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="SemiBold" />
            <TextBlock Text="{Binding LocalAppDataDirDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,10" />
            <Grid Margin="0,0,0,22">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="92" /><ColumnDefinition Width="82" /></Grid.ColumnDefinitions>
              <TextBox Grid.Column="0" Text="{Binding LocalAppDataDirOverride, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="34" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" Foreground="{DynamicResource SettingsText}" CaretBrush="{DynamicResource SettingsCaret}" SelectionBrush="{DynamicResource SettingsSelection}" Padding="10,0" VerticalContentAlignment="Center" FontFamily="{DynamicResource IpiCodeFontFamily}" ToolTip="{Binding EffectiveLocalAppDataDir}" />
              <Button Grid.Column="1" Margin="10,0,0,0" Height="34" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="BrowseLocalAppDataDir_Click"><TextBlock Text="{Binding BrowseText}" Foreground="{DynamicResource SettingsText}" FontSize="12" /></Button>
              <Button Grid.Column="2" Margin="10,0,0,0" Height="34" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="ResetLocalAppDataDir_Click"><TextBlock Text="{Binding RestoreDefaultText}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" /></Button>
            </Grid>

            <Border Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" CornerRadius="7" Padding="12,10" Margin="0,0,0,16">
              <TextBlock Text="{Binding StorageRestartNotice}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
            </Border>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
              <TextBlock Text="{Binding StorageStatus}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" VerticalAlignment="Center" Margin="0,0,14,0" Visibility="{Binding StorageStatusVisibility}" />
              <Button Width="112" Height="34" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="SaveStoragePaths_Click"><TextBlock Text="{Binding SaveStoragePathsText}" Foreground="{DynamicResource SettingsText}" FontSize="13" /></Button>
            </StackPanel>
          </StackPanel>
        </Border>
      </Grid>

      <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding RuntimeDiagnosticsPageVisibility}" Margin="52,44,62,36">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
              <local:LucideIcon Icon="terminal-square" Size="18" Stroke="{DynamicResource SettingsMuted}" Margin="0,1,12,0" />
              <TextBlock Text="{Binding RuntimeDiagnosticsTitle}" Foreground="{DynamicResource SettingsText}" FontSize="18" FontWeight="Medium" />
              <TextBlock Text="{Binding RuntimeDiagnosticsSummary}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="14,4,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding RuntimeDiagnosticsDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
          </StackPanel>
          <Button Grid.Column="1" Width="92" Height="32" Style="{StaticResource SettingsGhostButton}" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" Click="RefreshDiagnostics_Click">
            <TextBlock Text="{Binding RefreshDiagnosticsText}" Foreground="{DynamicResource SettingsText}" FontSize="13" />
          </Button>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource SettingsCard}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="1" CornerRadius="9" ClipToBounds="True">
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding RuntimeDiagnostics}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,0,0,1" Padding="18,14">
                    <Grid>
                      <Grid.ColumnDefinitions><ColumnDefinition Width="74" /><ColumnDefinition Width="220" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                      <Border Width="52" Height="24" CornerRadius="12" Background="{DynamicResource SettingsControlBg}" BorderBrush="{DynamicResource SettingsControlBorder}" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                        <TextBlock Text="{Binding Badge}" Foreground="{DynamicResource SettingsMuted}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Border>
                      <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" TextTrimming="CharacterEllipsis" Margin="0,2,18,0" />
                      <TextBlock Grid.Column="2" Text="{Binding Detail}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" TextWrapping="Wrap" />
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Border>
      </Grid>

      <Border Grid.Row="2" Grid.Column="1" Background="{DynamicResource SettingsFooterBg}" BorderBrush="{DynamicResource SettingsLine}" BorderThickness="0,1,0,0" Padding="52,0,62,0" Visibility="{Binding AppearancePageVisibility}">
        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="280" /><ColumnDefinition Width="48" /></Grid.ColumnDefinitions>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="{Binding WindowTransparencyTitle}" Foreground="{DynamicResource SettingsText}" FontSize="14" FontWeight="Medium" />
            <TextBlock Text="{Binding WindowTransparencyDescription}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" Margin="0,6,0,0" />
          </StackPanel>
          <Slider Grid.Column="1" Minimum="0" Maximum="70" Value="{Binding WindowTransparency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
          <TextBlock Grid.Column="2" Text="{Binding WindowTransparencyLabel}" Foreground="{DynamicResource SettingsMuted}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right" />
        </Grid>
      </Border>
    </Grid>
  </Border>
</Window>
